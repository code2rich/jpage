version: '3.8'

services:
  app:
    build: .
    container_name: html-markdown-viewer
    ports:
      - "8858:8858"
    volumes:
      - ./data:/app/data
    environment:
      - NODE_ENV=production
      - PORT=8858
    restart: unless-stopped
