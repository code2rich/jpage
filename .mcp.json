{
  "_comment": "Example MCP client config for Claude Code / Claude Desktop. Enable by either: (1) placing this file at the repo root, or (2) merging the mcpServers block into ~/.claude.json (Claude Code) or ~/Library/Application Support/Claude/claude_desktop_config.json (Claude Desktop). ${env.MCP_TOKEN} is read from the launching shell's env. Remove the _comment key after copying.",
  "mcpServers": {
    "jpage": {
      "type": "http",
      "url": "http://localhost:8858/mcp",
      "headers": {
        "Authorization": "Bearer ${env.MCP_TOKEN}"
      }
    }
  }
}
