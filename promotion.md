# 即页 —— Agent 时代，HTML 输出的最佳归宿

你有没有过这种无奈的经历？

Claude Code 辛辛苦苦生成了一份精美的 HTML 报告——带交互图表、带折叠面板、带响应式布局。你兴奋地想发给同事看看，结果……

> "发你个 HTML 文件，你下载后用浏览器打开。"
>
> "这文件里引了本地 CSS，你那边样式全崩了。"
>
> "要不我截个图给你？"

**一份本应惊艳的 AI 生成作品，最后沦为一张静态截图。**

与此同时，你的 Markdown 笔记倒是随便往哪个平台一贴就能看。可但凡遇到 AI 生成的复杂 HTML 页面——原型、数据看板、交互文档——分享成本陡然飙升。

**部署到服务器？** 太小题大做。
**用 GitHub Pages？** 仓库、分支、Actions，仪式感拉满。
**直接发文件？** 对方打开就是一团乱码。

**AI 的输出已经进化成「轻量级应用」了，但我们的分享方式还停留在「发文档」的阶段。**

这正是 **即页** 想解决的问题。

---

## 一、Agent 输出正在 HTML 化，分享却还在石器时代

最近 Claude Code 工程师 Thariq Shihipar 写了一篇爆文，核心观点很简单：

> **Markdown 已经越来越限制 Agent 的表达能力。HTML 才是 Agent 时代人与 AI 协作的更优格式。**

为什么？

| Markdown 能给的 | HTML 能给 |
|---|---|
| 纯文本标题 | CSS 弹性布局、网格定位 |
| ASCII 伪图表 | 真正的 SVG 矢量图、Canvas 可视化 |
| 代码块 | 可交互的 JS Playground |
| 静态表格 | 带排序、筛选的数据表格 |
| 无颜色 | 真实色值、渐变、动画 |

AI 的输出正在从「文档」进化为「界面」。但界面的前提，是**能被浏览器正确渲染、能被他人方便访问**。

即页只做一件事：**拖入 HTML，即刻成页。**

---

## 二、即页是什么？一句话说清楚

**即页 = 零配置的 HTML / Markdown 即时预览与分享工具。**

写完文档、生成报告、做完原型，直接拖进来，秒级获得一个干净的在线链接——无需部署流程，无需服务器知识。

它长这样：

- 一个清爽的上传区域，拖拽即传
- 文件列表卡片式展示，一目了然
- 点击文件进入预览，渲染 / 源码双模式一键切换
- 支持重命名、删除、下载，操作简单直观

没有账户体系，没有学习成本，打开即用。

---

## 三、三个核心场景：即页解决的是真问题

### 场景一：AI 生成的 HTML 报告，一键分享

用 Claude Code 生成了一份架构分析 HTML，包含 SVG 图表、交互折叠面板、代码高亮。拖入即页，3 秒后获得链接，直接甩到 Slack / 微信 / 邮件里。

对方点开链接，在浏览器里看到的就是**完整渲染的交互页面**，不是截图，不是附件。

### 场景二：Markdown 笔记快速托管

本地写好的 Markdown 笔记、技术文档、会议纪要，拖进来自动渲染为排版精美的页面。临时分享给团队评审，用完即走。

支持 `.md` `.markdown` `.html` `.htm`，单文件最大 50MB，大部分场景够用。

### 场景三：静态页面原型托管

前端写了个单页 HTML Demo，想发给产品经理或设计师看效果。不用开 Vercel、不用配 GitHub Pages，拖入即页，链接到手。

---

## 四、部署极简：Docker 一行命令，本地双击也能跑

即页的设计哲学是「零依赖运行」。

### 方式一：Docker 一键启动（推荐）

```bash
git clone https://github.com/code2rich/jpage.git
cd jpage
docker-compose up -d
```

访问 http://localhost:3000，即刻使用。

### 方式二：本地运行

```bash
npm install
npm start
```

无需数据库配置，SQLite 内置存储。无需前端构建，原生 JavaScript 直接跑。

技术栈干净到离谱：

| 层级 | 技术 | 说明 |
|---|---|---|
| 后端 | Node.js + Express | 轻量稳定 |
| 数据库 | SQLite3 | 零配置，开箱即用 |
| 前端 | 原生 JavaScript | 无框架依赖，加载飞快 |
| 渲染 | marked.js | Markdown 转 HTML |
| 容器 | Docker / Docker Compose | 一键部署 |

---

## 五、对比：即页 vs 现有方案

你可能想：这功能用别的工具也能凑合，有必要专门做一个吗？

咱们摊开说：

| 对比维度 | 即页 | GitHub Gist | 语雀/Notion | Vercel/Netlify |
|---|---|---|---|---|
| **核心定位** | HTML/Markdown 即时预览分享 | 代码片段托管 | 文档知识库 | 全站部署平台 |
| **使用成本** | 零配置，拖入即用 | 需 GitHub 账号 | 需注册、配权限 | 需仓库、CI、域名 |
| **HTML 渲染** | ✅ 完整渲染 | ❌ 仅代码高亮 | ❌ 仅嵌入预览 | ✅ 完整渲染 |
| **Markdown** | ✅ 自动渲染 | ✅ 支持 | ✅ 原生支持 | ❌ 需静态生成 |
| **自托管** | ✅ Docker 一键 | ❌ 托管平台 | ❌ 托管平台 | ❌ 托管平台 |
| **源码/渲染切换** | ✅ 一键切换 | ❌ 无 | ❌ 无 | ❌ 无 |
| **数据隐私** | ✅ 本地存储 | ⚠️ 云端 | ⚠️ 云端 | ⚠️ 云端 |
| **上手门槛** | 🟢 极低 | 🟡 中 | 🟡 中 | 🔴 高 |

**即页不是取代谁，而是填补了一个空白：当你只想快速分享一份 HTML 或 Markdown，不想折腾任何配置的时候。**

---

## 六、HTML 是趋势，即页是配套

Thariq 在他的文章末尾说了一句话：

> *"我已经越来越少亲自编辑这些文件了。"*

当 Agent 成为主要生产者，人类更多是**阅读、决策、转发**。这意味着：

- **生产端**：AI 输出 HTML（ richer，更 expressive ）
- **消费端**：人类通过浏览器阅读 HTML（直观，可交互）
- **中间层**：需要一种**零摩擦**的方式，把 AI 的 HTML 产出传递给人类

即页就是这个中间层。

它不做 AI 生成，不做复杂协作，只专注一个环节：**让 HTML 和 Markdown 的分享回归简单。**

---

## 七、写在最后

Agent 时代的文档正在发生一件根本性转变：

> **从「文本」到「界面」。**

Markdown 不会死，但会越来越像中间格式——Agent 内部通信用，最终给人看的越来越 HTML 化。

而当 HTML 成为主流输出格式时，我们迫切需要一种**轻量、即时、零配置**的分享方式。

**即页**就是为此而生。

一行命令启动，拖入文件即用。AI 生成的精美 HTML，值得被完整呈现，而不是沦为截图。

---

**开源地址**：github.com/code2rich/jpage

**授权协议**：MIT 开源，免费商用、无功能限制

觉得方向靠谱欢迎提前关注，上线后第一时间体验。

💬 如果你也有「HTML 文件分享困难」的痛点，欢迎在评论区聊聊你的场景。
